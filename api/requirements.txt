fastapi
uvicorn[standard]
# ChromaDB i sentence-transformers wyłączone - za dużo pamięci dla render.com (>512MB)
# Lokalnie odkomentuj jeśli chcesz używać wyszukiwania semantycznego (ustaw ENABLE_CHROMADB=true)
# chromadb
# sentence-transformers
